<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="kr.co.teamo.apply.mapper.ApplyMapper">

	<!-- ResultMap에 typeHandler 지정 -->
	<resultMap id="ApplyResultMap" type="ApplyResponseDto" autoMapping="true">
	    <result property="techStackCd"        column="TECH_STACK_CD"        typeHandler="StringListTypeHandler"/>
	</resultMap>

    <!-- 지원 등록 -->
    <insert id="insertApply" parameterType="ApplyRequestDto">
        <selectKey keyProperty="applyId" resultType="long" order="BEFORE">
            SELECT POST_APPLY_SEQ.NEXTVAL FROM DUAL
        </selectKey>
        INSERT INTO POST_APPLY (
            APPLY_ID                /* 지원 아이디 */
          , POST_ID                 /* 게시글 아이디 */
          , USER_ID                 /* 사용자 아이디 */
          , RECRUIT_POSIT_TYPE_CD   /* 지원 포지션 코드 */
          , TECH_STACK_CD           /* 기술스택 (콤마 구분) */
          , APPLY_REASON            /* 지원 동기 */
          , PORTFOLIO_URL           /* 포트폴리오 URL */
          , STATUS_CD               /* 지원 상태 코드 (10: 대기) */
          , REG_DT                  /* 등록일 */
        ) VALUES (
            #{applyId}
          , #{req.postId}
          , #{req.userId}
          , #{req.recruitPositTypeCd}
          , #{req.techStackCdStr}
          , #{req.applyReason}
          , #{req.portfolioUrl}
          , #{statusCd}
          , SYSDATE
        )
    </insert>

    <!-- 게시글별 지원 목록 조회 -->
    <select id="selectApplyListByPostId" resultType="ApplyResponseDto" resultMap="ApplyResultMap">
        SELECT
            A.APPLY_ID
          , A.POST_ID
          , A.USER_ID
          , U.NAME
          , A.RECRUIT_POSIT_TYPE_CD
          , A.TECH_STACK_CD
          , (SELECT DTL_CD_NM FROM CMM_CODE_DTL
             WHERE COM_CD_ID = 'RECRUIT_POSIT_TYPE_CD'
             AND DTL_CD_ID = A.RECRUIT_POSIT_TYPE_CD) AS RECRUIT_POSIT_TYPE_NM
          , A.APPLY_REASON
          , A.PORTFOLIO_URL
          , A.STATUS_CD
          , (SELECT DTL_CD_NM FROM CMM_CODE_DTL
             WHERE COM_CD_ID = 'APPLY_STATUS_CD'
             AND DTL_CD_ID = A.STATUS_CD) AS STATUS_NM
          , TO_CHAR(A.REG_DT, 'YYYY-MM-DD') AS REG_DT
          , A.STATUS_CD
        FROM POST_APPLY A
        JOIN USERS u
        ON A.USER_ID = U.USER_ID
        WHERE A.POST_ID = #{postId}
        AND A.USER_ID != (SELECT P.USER_ID FROM POST P WHERE P.POST_ID = #{postId}) /* 스터디장은 목록에서 제외 */
        ORDER BY A.REG_DT DESC
    </select>

    <select id="findByApplyId" parameterType="ApplyRequestDto" resultType="ApplyResponseDto">
    	SELECT
    		   APPLY_ID
    		 , POST_ID
    		 , USER_ID
    		 , STATUS_CD
       FROM POST_APPLY
       WHERE APPLY_ID = #{applyId}
    </select>

    <!-- 중복 지원 여부 확인 -->
    <select id="countApply" resultType="int">
        SELECT COUNT(*)
        FROM POST_APPLY
        WHERE POST_ID = #{postId}
          AND USER_ID = #{userId}
    </select>

    <update id="updateApplyStatus" parameterType="ApplyRequestDto">
    	UPDATE POST_APPLY
		SET STATUS_CD = #{statusCd}
		WHERE APPLY_ID = #{applyId}
    </update>

</mapper>
