spring:
  config:
    activate:
      on-profile: local
  datasource:
    url: jdbc:p6spy:oracle:thin:@yggcv8zldawsd5fk_medium?TNS_ADMIN=C:/wallet
    username: dev
    password: Jaeyeong1234
    driver-class-name: com.p6spy.engine.spy.P6SpyDriver
  data:
    redis:
      host: localhost
      port: 6379
  servlet:
    multipart:
     max-file-size: 10MB
     max-request-size: 20MB

server:
  port: 9090

decorator:
  datasource:
    p6spy:
      enable: false

logging:
  level:
    p6spy: INFO
    kr.co.teamo: DEBUG                               # 서비스/매퍼 레벨 로그 활성화
    org.springframework.security: DEBUG              # Security 필터 흐름 전체 출력
    org.springframework.web.servlet.mvc: DEBUG       # MVC 경로 매핑 확인용

file:
  upload:
    path: C:/upload
    server: /upload
app:
  frontend-url: http://localhost:3000
    